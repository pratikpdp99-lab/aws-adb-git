# Databricks
DATABRICKS_HOST=https://<your-workspace>.cloud.databricks.com
DATABRICKS_TOKEN=<your-personal-access-token>

# AWS (credentials from ~/.aws/credentials via `aws configure`)
AWS_REGION=us-east-1
