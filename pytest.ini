[pytest]
testpaths = backend/tests databricks/tests
markers =
    unit: pure unit tests (no network, no Spark)
    integration: requires running backend
    databricks: requires PySpark
    guardrail: PII/data-leakage guardrail tests
    slow: long-running tests
filterwarnings = ignore::DeprecationWarning
asyncio_mode = auto
